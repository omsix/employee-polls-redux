import { render } from "@testing-library/react";
import { PollDetailsComponent } from "./poll-details.component";

describe("PollDetailsComponent", () => {
  it("", () => {
    render(<PollDetailsComponent />);
  });
});