import { render, screen } from "@testing-library/react";
import { CircularTextComponent } from "./circular-text.component";

describe("CircularTextComponent", () => {
  it("", () => {
    render(<CircularTextComponent />);
  });
});