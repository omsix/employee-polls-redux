import { render } from "@testing-library/react";
import MenuToolbarComponent from "./menu-toolbar.component";

describe("MenuToolbarComponent", () => {
  it("", () => {
    render(<MenuToolbarComponent />);
  });
});